import React from 'react';

const title = (props) => {
    return (
        <div>
            <div>
                <h1>{props.content}</h1>
            </div>
        </div>
    );
}

export default title;